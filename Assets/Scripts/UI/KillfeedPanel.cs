using UnityEngine;

namespace Game
{
    public class KillfeedPanel : MonoBehaviour
    {
        [SerializeField]
        private KillfeedElement _elementPrefab;

        private void Start()
        {

            //Instantiate(_elementPrefab, transform).Initialize(vicitim, killer);
        }
    }
}
